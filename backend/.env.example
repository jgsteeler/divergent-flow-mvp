# Backend environment variables (local dev)
# This file is gitignored by the repo-wide .gitignore.

# CORS
# Comma-separated list of allowed Origins (scheme://host[:port]).
# Example: http://localhost:5173,https://app.getdivergentflow.com
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Redis (Upstash)
# Redis connection URL (without scheme or credentials)
# Example: fly-div-flo-staging.upstash.io:6379
REDIS_URL=your-redis-host:6379

# Redis authentication token
# Get this from your Upstash dashboard
# Leave empty for local Redis without authentication
REDIS_TOKEN=your-redis-token

# Optional: force Redis TLS on/off.
# Recommended for Upstash: true
# Recommended for local Redis: false (or leave unset)
REDIS_SSL=true
