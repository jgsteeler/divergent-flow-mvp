# Backend environment variables (local dev)
# This file is gitignored by the repo-wide .gitignore.

# CORS
# Comma-separated list of allowed Origins (scheme://host[:port]).
# Example: http://localhost:5173,https://app.getdivergentflow.com
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Type Inference Background Service
# These settings are optional and will use defaults from appsettings.json if not specified.
# TypeInference__ConfidenceThreshold: Confidence threshold (0-1) below which captures will be re-inferred (default: 0.95)
# TypeInference__ProcessingIntervalSeconds: Interval in seconds between background re-inference runs (default: 60)
# TypeInference__ConfidenceThreshold=0.95
# TypeInference__ProcessingIntervalSeconds=60

# Redis

# Local Docker Redis (TCP)
# - When using local docker-compose Redis, set:
#   REDIS_URL=redis:6379
#   REDIS_TOKEN=
#   REDIS_SSL=false

# Upstash (REST)
# Upstash recommends using the HTTPS REST URL from the console.
# Example: https://us1-merry-cat-32748.upstash.io
UPSTASH_REDIS_REST_URL=https://your-db-name.upstash.io

# Upstash REST tokens (Bearer)
# - Standard token: full read/write
# - Readonly token: read-only (optional)
UPSTASH_REDIS_REST_TOKEN=your-upstash-rest-token
UPSTASH_REDIS_REST_READONLY_TOKEN=

# Back-compat:
# If you already have REDIS_URL/REDIS_TOKEN set for Upstash REST, the code will still use them.
REDIS_URL=
REDIS_TOKEN=

# If you need to force REST for a non-upstash hostname (rare), set true.
REDIS_USE_REST=false
