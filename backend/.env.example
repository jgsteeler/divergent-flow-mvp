# Backend environment variables (local dev)
# This file is gitignored by the repo-wide .gitignore.

# MongoDB
# MongoDB connection string and database name
# These settings are for environments that do not have appsettings.json support.
# Examples:
# MongoDB__ConnectionString=mongodb://localhost:27017
# MongoDB__DatabaseName=divergent_flow
# Optional: customize collection names (defaults shown)
# MongoDB__ItemsCollectionName=items
# MongoDB__CollectionsCollectionName=collections

# CORS
# Comma-separated list of allowed Origins (scheme://host[:port]).
# Example: http://localhost:5173,https://app.getdivergentflow.com
# CORS_ALLOWED_ORIGINS=http://localhost:5173

# Inference Background Service
# These settings are optional and will use defaults from appsettings.json if not specified.
# Inference__ConfidenceThreshold: Confidence threshold (0-100) below which captures will be re-inferred (default: 95)
# Inference__ProcessingIntervalSeconds: Interval in seconds between background re-inference runs (default: 60)
# Inference__ConfidenceThreshold=95
# Inference__ProcessingIntervalSeconds=60

# Redis
# Upstash (REST)
# If you want to use Upstash REST instead of local Redis, uncomment these and remove REDIS_URL above.
# UPSTASH_REDIS_REST_URL=<your-upstash-rest-url-here>
# UPSTASH_REDIS_REST_TOKEN=<your-upstash-rest-token-here>
# Optional: readonly token (recommended if you need a read-only client)
# UPSTASH_REDIS_REST_READONLY_TOKEN=<your-readonly-token-here>
