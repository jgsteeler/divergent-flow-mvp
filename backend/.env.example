# Backend environment variables (local dev)
# This file is gitignored by the repo-wide .gitignore.

# MongoDB
# MongoDB connection string and database name
# Examples:
# - Local MongoDB: mongodb://localhost:27017
# - MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_CONNECTION_STRING=mongodb://localhost:27017
MONGODB_DATABASE_NAME=divergent_flow
# Optional: customize collection names (defaults shown)
# MONGODB_ITEMS_COLLECTION=items
# Redis (Upstash REST)
# Copy these values from the Upstash console.
# Example: https://us1-merry-cat-32748.upstash.io
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Optional: readonly token (recommended if you need a read-only client)
UPSTASH_REDIS_REST_READONLY_TOKEN=
# Comma-separated list of allowed Origins (scheme://host[:port]).
# Example: http://localhost:5173,https://app.getdivergentflow.com
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Type Inference Background Service
# These settings are optional and will use defaults from appsettings.json if not specified.
# TypeInference__ConfidenceThreshold: Confidence threshold (0-100) below which captures will be re-inferred (default: 95)
# TypeInference__ProcessingIntervalSeconds: Interval in seconds between background re-inference runs (default: 60)
# TypeInference__ConfidenceThreshold=95
# TypeInference__ProcessingIntervalSeconds=60
